// WAP to print area of triangle  ---- 1/2 * b * h

var base = parseInt(prompt("Enter Base"))
var height=parseInt(prompt("Enter Height"))

var aot= 0.5*base*height

document.write("Area of Triangle ="+aot)